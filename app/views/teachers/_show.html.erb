<strong>Преподаватель:</strong>
  <%= @teacher.name %>
  <br>
  <%= image_tag @teacher.photo.url(:large) %>
  <p>
  <%= simple_format @teacher.biography%>
  <strong>Ведет группы:</strong>
    В разработке
