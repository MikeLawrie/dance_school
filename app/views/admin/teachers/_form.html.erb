<%= form_for [:admin, @teacher] do |f| %>
  <%= render 'common/errors', resource: @teacher %>  

  <div class="field">
    <%= f.label :name, "Фамилия и имя" %>
    <%= f.text_field :name %>
  </div>



  <div class="actions">
    <%= f.submit "#{@teacher.new_record? ? 'Создать' : 'Изменить' }" %>
  </div>
<% end %>